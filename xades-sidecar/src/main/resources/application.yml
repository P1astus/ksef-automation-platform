server:
  port: 8090

spring:
  application:
    name: ksef-xades-sidecar

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

ksef:
  environment: ${KSEF_ENVIRONMENT:test}
